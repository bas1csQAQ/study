#include <stdio.h>
#include <string.h>
// 编写程序，求两个数的最大公约数及最小公倍数，用函数完成（即求最大公约数为一个函数，求最小公倍数为一个函数完成）。

int maxGongyueshu(int a, int b) {
	int num1 = a>b?b:a;
	int num2 = a+b-num;
	int i;
	if(num%num1==0)
		return num1;
	for(;num>0;num--){
		if()
	}
} 

int minGongbeishu(int a, int b) {
	
}

int	main()
{
	int a,b;
	scanf("%d,%d",a,b);
	
	return 0;
}
